---

- name: create docker configuration directories
  file:
    path: "~/appdata/{{ item }}"
    state: directory
    recurse: true
  loop:
    - jellyfin
    - kiwix
    - mariadb
    - podgrab
    - diun
    - scrutiny
    - freshrss
    - uptime-kuma
  tags: compose
