---

- name: Update apt cache
  package:
    update_cache: true
    cache_valid_time: 3600

- name: Ensure dependencies are installed
  package:
    name:
      - python3
      - ffmpeg
    state: present
